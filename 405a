#include <iostream>
using namespace std;
#include <bits/stdc++.h>
int main() {
	// your code goes here
	int n, arr[1000];
	cin>>n;
	for (int i=0;i<1000;i++)
	{
		arr[i]=200;
	}
	for (int j=0;j<n;j++)
	{
		cin>>arr[j];
	}
	int p = sizeof(arr)/sizeof(arr[0]);
	//cout<<n<<endl;
	//cout<<arr[0]<<endl;
		//cout<<arr[1];
	
 
    sort(arr, arr+p);
   // cout<<arr[0];
    	for (int k=0;k<n;k++)
	{ //if(k!=n)
	    if(k<n-1)
	    {
		cout<<arr[k];
		cout<<" ";
	    }
		else
		cout<<arr[k];
	  //else
	  // cout<<arr[k];
	}
	return 0;
}
