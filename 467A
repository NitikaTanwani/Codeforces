#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int live[100],capacity[100],n,count=0;
	cin>>n;
	for(int i=0;i<n;i++)
	{
		cin>>live[i];
		cin>>capacity[i];
	}
	for(int j=0;j<n;j++)
	{
		if(capacity[j]-live[j]>=2)
		count++;
	}
	cout<<count;
	
	return 0;
}
